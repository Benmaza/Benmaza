total_times = 0
total_times = total_times + 1
print(total_times)

sold_times = 0
sold_times = sold_times + 1
total_times1 = total_times + sold_times
print(total_times1)

# ¿ como crear un dataframe con dos columnas y dos filas ?